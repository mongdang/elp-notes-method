# {repoName} — 에이전트 지침 포인터

> [!CAUTION]
> **작업 전 필수 확인** — `{notesPrefix}.method/RULES.md` 를 **끝까지 읽은 뒤에만** 작업한다.
> 그 파일이 없으면 작업을 중단하고 사용자에게 알린다. 규칙이 적용되지 않은 채로 기록이
> 쌓이면 나중에 알아낼 방법이 없다.

규칙 전문은 `{notesPrefix}.method/RULES.md` 하나가 원본이다. 이 파일에 규칙을 옮겨 적지
않는다 — 사본을 두면 반드시 어긋난다.

이 저장소 고유 규칙은 `{notesPrefix}CLAUDE.md` 의 "이 저장소 고유 규칙" 절 참고.

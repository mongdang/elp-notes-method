# 기여 안내

이 저장소의 존재 이유는 **규칙이 한 곳에만 존재하게** 하는 것이다. 같은 문장을 두 파일에
쓰고 있다면, 한쪽은 포인터가 되어야 한다.

## 규칙 개정이 각 저장소까지 도달하는 길

```
skills/<이름>/SKILL.md 를 고친다        규칙은 여기, 여기에만 있다
  → plugin.json 의 version 을 올린다    필수 — 아래 참고
  → push
  → 각 저장소에서 /notes               .method/ 를 다시 쓰고 새 VERSION 을 커밋
```

`.method/` 를 사람이 손으로 고치지 않는다. 그게 애초에 사본이 갈라진 이유이고, 스냅샷이
자기 VERSION 과 안 맞으면 CI가 실패한다.

> [!IMPORTANT]
> **배포되는 변경마다 `.claude-plugin/plugin.json` 의 `version` 을 올린다.**
> 설치본은 버전 디렉터리에 캐시되므로, 버전을 그대로 두면 이미 설치한 머신은 처음 받은
> 사본을 계속 쓴다. 마켓플레이스를 갱신해도 바뀌지 않는다. 이걸 몰라서 반나절을 썼다.

[CHANGELOG.md](CHANGELOG.md) 에 무엇이 바뀌었는지 적는다. 이전이 틀렸던 것이라면 **무엇이
깨져 있었는지**까지 적는다 — 그 문장이 규칙보다 오래 쓸모 있다.

## 테스트

```bash
pip install -r requirements-dev.txt
python -m pytest -q
```

**테스트를 먼저 쓴다.** 검사기의 모든 항목은 실제로 커밋까지 도달한 실수 때문에 생겼다.
실제 실패를 서술하지 않는 테스트는 둘 값어치가 없다 — 이름을 함수가 아니라 **실패**로 짓는다.

CI는 Linux와 Windows 양쪽에서 돈다. 줄바꿈, 콘솔 인코딩, 인터프리터 탐색 — 셋 다 두 환경에서
다르게 동작했고, 그 차이를 전부 사용자가 아니라 테스트가 먼저 찾았다.

## 이 저장소는 공개다

비공개 프로젝트에서 추출한 것이므로, 거기서 가져오는 것은 **일반화한 뒤에** 들어온다:
사내 호스트·실명·이메일·머신 경로 금지. CI가 그걸 grep 하지만 grep은 최후 방어선이다 —
push 되고 나면 공개 이력이다.

## 규칙을 추가할 때

추가하기 전에, 그게 여기에 속하는지 먼저 본다.

| 플러그인에 속하는 것 | 저장소에 속하는 것 |
|---|---|
| 절차 · 형식 · 검사 | 현황판, 결정 기록, 게이트 항목 |
| 안전 게이트를 **운영하는 법** | 어떤 항목이 열려 있고 누가 확인했는지 |
| 병행 작업을 **병합하는 법** | 작업자가 누구인지 |

한 프로젝트에만 해당하는 것이면 그 프로젝트의 `CLAUDE.md` 로 간다.

## 글쓰기

규칙은 **새벽 두 시에 그걸 따를 사람**을 위해 쓴다. 규칙을 먼저 쓰고, 그다음에 그게 왜
있는지를 쓴다 — 사람들이 우회하지 않게 만드는 건 규칙이 아니라 이유다. 실제 사고에서 나온
규칙이면 그 사실을 적는다. 그 한 문장이 규칙 본문보다 일을 더 한다.
